<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Login — Campo do Gado</title>
  <link rel="stylesheet" href="/css/style.css">
  <style>
    .login-body {
      min-height:100vh;
      display:flex;
      align-items:center;
      justify-content:center;
      background-image: url('/img/login-bg.png');
      background-size: contain;
      background-position: top center;
      background-repeat: no-repeat;
      background-color: #f3f6f5;
      padding-top: 220px; /* empurra o card abaixo do logo */
    }
    .login-container { width:100%; max-width:420px; background:#fff; border-radius:10px; padding:22px; box-shadow:0 10px 30px rgba(0,0,0,0.06); }
    .login-title { margin-bottom:10px; color:var(--green-700); font-size:20px; }
    .login-card input { width:100%; padding:8px; margin-bottom:10px; border-radius:6px; border:1px solid #dfe7e4; }
    .login-card .btn-save { width:100%; display:block; }
    .login-forgot { display:block; margin-top:8px; color:#4a2b84; text-decoration:underline; }
  </style>
</head>
<body class="login-body">
  <div class="login-container">
    <h2 class="login-title">Acesso ao Sistema</h2>

    <% if (error) { %>
      <div class="alert alert-error"><%= error %></div>
    <% } %>

    <form class="login-card" method="POST" action="/login">
      <label>Usuário</label>
      <input type="text" name="usuario" required>

      <label>Senha</label>
      <input type="password" name="senha" required>

      <button class="btn-save">Entrar</button>
    </form>

    <a href="/forgot" class="login-forgot">Esqueci minha senha</a>
  </div>
</body>
</html>
